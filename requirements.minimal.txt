# Minimal dependencies for faster builds
# Core FastAPI stack
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.4.2

# Prefect (without heavy extras)
prefect==2.14.21

# Snowflake connector
snowflake-connector-python==3.12.3

# Essential utilities
python-dotenv==1.0.0
python-multipart==0.0.6